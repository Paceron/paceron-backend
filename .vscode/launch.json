{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start paceron backend",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/cmd/api",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "args": [],
      "console": "integratedTerminal"
    }
  ]
}
