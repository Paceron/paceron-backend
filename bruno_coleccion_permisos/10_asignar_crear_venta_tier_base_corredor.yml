info:
  name: asignar crear_venta a tier base corredor
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:8080/api/v1/tiers/1/permissions
  body:
    type: json
    data: |-
      {
        "permission_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
