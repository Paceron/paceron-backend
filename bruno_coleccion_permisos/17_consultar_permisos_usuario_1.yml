info:
  name: consultar permisos usuario 1
  type: http
  seq: 17

http:
  method: GET
  url: http://localhost:8080/api/v1/auth/permissions?user_id=1
  params:
    - name: user_id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
