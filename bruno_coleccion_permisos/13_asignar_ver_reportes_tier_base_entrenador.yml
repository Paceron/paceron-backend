info:
  name: asignar ver_reportes a tier base entrenador
  type: http
  seq: 13

http:
  method: POST
  url: http://localhost:8080/api/v1/tiers/3/permissions
  body:
    type: json
    data: |-
      {
        "permission_id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
