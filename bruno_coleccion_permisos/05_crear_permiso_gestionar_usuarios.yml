info:
  name: crear permiso gestionar usuarios
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/api/v1/permissions
  body:
    type: json
    data: |-
      {
        "name": "gestionar_usuarios",
        "description": "Permiso para gestionar usuarios"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
