info:
  name: reset password v1
  type: http
  seq: 17

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/reset-password
  body:
    type: json
    data: |-
      {
        "email": "pepa@lota.com",
        "code": "REPLACE_WITH_CODE_FROM_EMAIL",
        "new_password": "Abcd-12345",
        "confirm_password": "Abcd-12345"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
