info:
  name: 7 - [Opcional] Simular webhook MP
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/api/v1/payments/webhook
  headers:
    - name: X-Signature
      value: dummy-signature-for-testing
    - name: X-Request-Id
      value: test-request-123
  body:
    type: json
    data: |-
      {
        "id": 1350795217,
        "live_mode": false,
        "type": "payment",
        "action": "payment.created",
        "data": {
          "id": "{{payment_id}}"
        }
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true