info:
  name: crear rol entrenador
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/v1/roles
  body:
    type: json
    data: |-
      {
        "name": "entrenador",
        "description": "Rol de entrenador"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
