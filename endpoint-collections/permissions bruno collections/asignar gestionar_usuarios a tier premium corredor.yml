info:
  name: asignar gestionar_usuarios a tier premium corredor
  type: http
  seq: 14

http:
  method: POST
  url: http://localhost:8080/api/v1/tiers/4/permissions
  body:
    type: json
    data: |-
      {
        "permission_id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
