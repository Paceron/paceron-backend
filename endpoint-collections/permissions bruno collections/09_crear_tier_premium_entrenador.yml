info:
  name: crear tier premium entrenador
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8080/api/v1/tiers
  body:
    type: json
    data: |-
      {
        "name": "premium",
        "description": "Tier premium para entrenador",
        "role_id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
