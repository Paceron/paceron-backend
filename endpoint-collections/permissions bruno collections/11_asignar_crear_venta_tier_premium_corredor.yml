info:
  name: asignar crear_venta a tier premium corredor
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:8080/api/v1/tiers/2/permissions
  body:
    type: json
    data: |-
      {
        "permission_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
