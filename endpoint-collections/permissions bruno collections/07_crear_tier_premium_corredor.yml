info:
  name: crear tier premium corredor
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/api/v1/tiers
  body:
    type: json
    data: |-
      {
        "name": "premium",
        "description": "Tier premium para corredor",
        "role_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
