info:
  name: crear permiso ver reportes
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/v1/permissions
  body:
    type: json
    data: |-
      {
        "name": "ver_reportes",
        "description": "Permiso para ver reportes"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
