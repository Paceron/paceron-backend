info:
  name: obtener permiso por id
  type: http
  seq: 29

http:
  method: GET
  url: http://localhost:8080/api/v1/permissions/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​