info:
  name: actualizar permiso crear_venta
  type: http
  seq: 20

http:
  method: PUT
  url: http://localhost:8080/api/v1/permissions/1
  body:
    type: json
    data: |-
      {
        "name": "crear_venta",
        "description": "Permiso para crear ventas - actualizado"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
