info:
  name: asignar rol corredor premium a usuario 1
  type: http
  seq: 16

http:
  method: POST
  url: http://localhost:8080/api/v1/users/1/roles
  body:
    type: json
    data: |-
      {
        "role_id": 1,
        "tier_id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
