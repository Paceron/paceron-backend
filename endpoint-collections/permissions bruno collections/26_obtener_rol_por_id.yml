info:
  name: obtener rol por id
  type: http
  seq: 26

http:
  method: GET
  url: http://localhost:8080/api/v1/roles/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​