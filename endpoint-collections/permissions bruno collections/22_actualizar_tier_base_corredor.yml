info:
  name: actualizar tier base corredor
  type: http
  seq: 22

http:
  method: PUT
  url: http://localhost:8080/api/v1/tiers/1
  body:
    type: json
    data: |-
      {
        "name": "base",
        "description": "Tier base para corredor - actualizado"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
