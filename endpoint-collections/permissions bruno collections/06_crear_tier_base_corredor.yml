info:
  name: crear tier base corredor
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/api/v1/tiers
  body:
    type: json
    data: |-
      {
        "name": "base",
        "description": "Tier base para corredor",
        "role_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
