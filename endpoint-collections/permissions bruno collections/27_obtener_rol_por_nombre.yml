info:
  name: obtener rol por nombre
  type: http
  seq: 27

http:
  method: GET
  url: http://localhost:8080/api/v1/roles/by-name?name=entrenador
  params:
    - name: name
      value: entrenador
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
