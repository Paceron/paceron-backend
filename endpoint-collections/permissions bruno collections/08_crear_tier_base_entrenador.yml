info:
  name: crear tier base entrenador
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8080/api/v1/tiers
  body:
    type: json
    data: |-
      {
        "name": "base",
        "description": "Tier base para entrenador",
        "role_id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
