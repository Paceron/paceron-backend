info:
  name: crear rol corredor
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/roles
  body:
    type: json
    data: |-
      {
        "name": "corredor",
        "description": "Rol de corredor"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
