info:
  name: Change Status user
  type: http
  seq: 14

http:
  method: PATCH
  url: http://localhost:8080/api/v1/users/1/status
  body:
    type: json
    data: |-
      {
        "status": "active"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​