info:
  name: forgot password v1
  type: http
  seq: 16

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/forgot-password
  body:
    type: json
    data: |-
      {
        "email": "pepa@lota.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
