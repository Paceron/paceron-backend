info:
  name: get user V1
  type: http

http:
  method: GET
  url: http://localhost:8080/api/v1/auth/user?id=1
  params:
    - name: id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
