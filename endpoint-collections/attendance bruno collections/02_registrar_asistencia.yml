info:
  name: 02 - Registrar asistencia
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/v1/attendance/team/1/session/1
  headers:
    - name: Authorization
      value: Bearer {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true