info:
  name: obtener grupo por id
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:8080/api/v1/groups/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
