info:
  name: agregar usuario con rol invalido (entrenador)
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/api/v1/teams/1/users
  body:
    type: json
    data: |-
      {
        "user_id": 2,
        "role_in_team": "entrenador"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
