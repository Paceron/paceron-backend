info:
  name: quitar usuario de grupo
  type: http
  seq: 15

http:
  method: DELETE
  url: http://localhost:8080/api/v1/groups/1/users/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
