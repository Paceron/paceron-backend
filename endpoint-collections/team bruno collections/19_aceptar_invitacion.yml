info:
  name: aceptar invitacion
  type: http
  seq: 19

http:
  method: POST
  url: http://localhost:8080/api/v1/invitations/1/accept
  body:
    type: json
    data: |-
      {
        "user_id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
