info:
  name: listar usuarios de un equipo
  type: http
  seq: 17

http:
  method: GET
  url: http://localhost:8080/api/v1/teams/1/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
