info:
  name: rechazar invitacion
  type: http
  seq: 20

http:
  method: POST
  url: http://localhost:8080/api/v1/invitations/1/reject
  body:
    type: json
    data: |-
      {
        "user_id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
