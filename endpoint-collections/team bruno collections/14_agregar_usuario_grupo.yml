info:
  name: agregar usuario a grupo
  type: http
  seq: 14

http:
  method: POST
  url: http://localhost:8080/api/v1/teams/1/groups/1/users
  body:
    type: json
    data: |-
      {
        "user_id": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
