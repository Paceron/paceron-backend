info:
  name: listar usuarios de un grupo
  type: http
  seq: 18

http:
  method: GET
  url: http://localhost:8080/api/v1/groups/1/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
