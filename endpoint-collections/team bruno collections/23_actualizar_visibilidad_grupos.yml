info:
  name: actualizar visibilidad de grupos a corredores
  type: http
  seq: 23

http:
  method: PUT
  url: http://localhost:8080/api/v1/teams/1
  body:
    type: json
    data: |-
      {
        "show_groups_to_runners": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
