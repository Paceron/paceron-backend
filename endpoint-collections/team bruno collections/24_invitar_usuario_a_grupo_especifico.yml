info:
  name: invitar usuario a equipo con grupo especifico
  type: http
  seq: 24

http:
  method: POST
  url: http://localhost:8080/api/v1/teams/1/invite
  body:
    type: json
    data: |-
      {
        "email": "corredor@test.com",
        "group_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
