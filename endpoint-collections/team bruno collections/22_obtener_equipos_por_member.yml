info:
  name: obtener equipos donde participo (member_id)
  type: http
  seq: 22

http:
  method: GET
  url: http://localhost:8080/api/v1/teams?member_id=1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
