info:
  name: listar invitaciones pendientes de un equipo
  type: http
  seq: 18

http:
  method: GET
  url: http://localhost:8080/api/v1/teams/1/invitations
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
