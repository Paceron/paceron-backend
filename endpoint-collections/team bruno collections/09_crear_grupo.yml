info:
  name: crear grupo
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8080/api/v1/groups
  body:
    type: json
    data: |-
      {
        "name": "Grupo Velocidad",
        "description": "Entrenamiento de velocidad",
        "team_id": 1,
        "is_main": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​